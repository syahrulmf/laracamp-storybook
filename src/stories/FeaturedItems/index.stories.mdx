import { Meta, Canvas, Story, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";

import FeaturedItems from "./example/Sample";

import "assets/css/index.css";

<Meta title="Components/FeaturedItems" />

# Featured Items

A most import component that simply let user interact with the app, [[Default Example](/docs/components-featured-items-example--success)] if you want to see in action

export const Template = (args) => <FeaturedItems {...args} />;
// Template.args = {
//   state: "success",
// };

<Story name="Introduction" args={{}}>
  {Template.bind({})}
</Story>
